---
alwaysApply: false
---

# ShifPita MVP Database Design Rules

このドキュメントは、ShifPita MVPにおけるデータベース設計の基本方針とテーブル定義を記述します。

- **DBMS**: SQLite (開発環境)
- **ORM**: Flask-SQLAlchemy

## 1. 設計思想・方針

本アプリケーションのデータベースは、以下の思想に基づいて設計されています。

- **ユーザー中心設計:**
  従来の従業員リストという考え方から、システムにログインする「ユーザー(`User`)」をデータモデルの中心に据えました。これにより、認証情報（パスワード等）と個人情報、役割(`Role`)、そしてそのユーザーに紐づく全ての活動（希望休申請、シフト割り当て）を一元的に管理できます。

- **履歴の保持:**
  いつ、誰が、どのシフトを生成したかを記録するため、「シフト生成履歴(`Transaction`)」テーブルを導入しました。これにより、過去に生成されたシフトをバージョンとして管理し、将来的な監査や再確認に対応できる拡張性を持たせています。個々のシフト(`Shift`)は、この`Transaction`に紐付きます。

- **正規化:**
  繰り返し発生するデータ（役割名、シフトのパターン名など）は、それぞれ`roles`、`shift_types`としてマスタテーブルに分離し、各テーブルからは外部キーで参照します。これにより、データの一貫性を保ち、変更を容易にしています。

- **柔軟性の確保:**
  シフト生成のルールなど、将来的に変更される可能性が高い固定値は `master` テーブルでキー・バリュー形式で管理し、コードの変更なしに設定を更新できる仕組みを目指します。

---

## 2. テーブル定義書

### 2.1. `roles` (役割マスタ)
従業員の役割を定義します。

| カラム名 | データ型 | PK | FK | Null | UQ | 説明 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| `id` | INTEGER | ✔ | | | | 主キー |
| `name` | VARCHAR(64) | | | | ✔ | 役割名 (例: `管理者`, `介護員`) |
| `can_night_shift`| BOOLEAN | | | | | 夜勤可否フラグ |
| `is_part_time` | BOOLEAN | | | | | パートタイマーフラグ |

### 2.2. `users` (ユーザー)
アプリケーションのログインユーザー。従業員と管理者の両方を含みます。

| カラム名 | データ型 | PK | FK | Null | UQ | 説明 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| `id` | INTEGER | ✔ | | | | 主キー |
| `username` | VARCHAR(64) | | | | ✔ | ログインID |
| `email` | VARCHAR(120) | | | ✔ | ✔ | メールアドレス |
| `password_hash` | VARCHAR(256) | | | | | ハッシュ化されたパスワード |
| `role_id` | INTEGER | | ✔ | | | `roles.id`への外部キー |

### 2.3. `shift_types` (シフトパターンマスタ)
勤務の種類を定義します。

| カラム名 | データ型 | PK | FK | Null | UQ | 説明 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| `id` | INTEGER | ✔ | | | | 主キー |
| `name` | VARCHAR(64) | | | | ✔ | シフト名 (例: `早1`, `夜1`) |
| `start_time` | TIME | | | | | 開始時刻 |
| `end_time` | TIME | | | | | 終了時刻 |

### 2.4. `day_off_requests` (希望休申請)
従業員からの希望休申請を記録します。

| カラム名 | データ型 | PK | FK | Null | UQ | 説明 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| `id` | INTEGER | ✔ | | | | 主キー |
| `request_date` | DATE | | | | | 希望休の日付 |
| `user_id` | INTEGER | | ✔ | | | `users.id`への外部キー |
| `created_at` | DATETIME | | | | | 申請日時 |

*複合ユニークキー: (`user_id`, `request_date`)*

### 2.5. `transactions` (シフト生成履歴)
シフト自動生成の実行履歴です。

| カラム名 | データ型 | PK | FK | Null | UQ | 説明 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| `id` | INTEGER | ✔ | | | | 主キー |
| `year` | INTEGER | | | | | 対象年 |
| `month` | INTEGER | | | | | 対象月 |
| `status` | VARCHAR(32) | | | | | 状態 (例: `success`, `failure`) |
| `created_at` | DATETIME | | | | | 生成実行日時 |

### 2.6. `shifts` (シフト)
生成されたシフトの一つ一つの割り当てを記録します。

| カラム名 | データ型 | PK | FK | Null | UQ | 説明 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| `id` | INTEGER | ✔ | | | | 主キー |
| `shift_date` | DATE | | | | | 勤務日 |
| `user_id` | INTEGER | | ✔ | | | `users.id`への外部キー |
| `shift_type_id` | INTEGER | | ✔ | | | `shift_types.id`への外部キー |
| `transaction_id`| INTEGER | | ✔ | | | `transactions.id`への外部キー |

*複合ユニークキー: (`user_id`, `shift_date`)*

### 2.7. `master` (マスター設定)
アプリケーション全体の固定設定値を管理します。

| カラム名 | データ型 | PK | FK | Null | UQ | 説明 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| `key` | VARCHAR(64) | ✔ | | | | 設定キー (例: `max_consecutive_work_days`) |
| `value` | VARCHAR(256)| | | | | 設定値 |
| `description` | TEXT | | | ✔ | | 設定の説明 |

---

## 3. 初期データ (Seed Data)

アプリケーションの初回起動時に、正常な動作に必須となる以下のマスタデータを投入します。

### 3.1. `roles` (役割)
| id | name |
|:---|:---|
| 1 | 管理者 |
| 2 | 介護員 |
| 3 | パート1 |
| 4 | パート2 |
| 5 | パート3 |
| 6 | パート4 |
| 7 | 責任者 |
| 8 | サポート |

### 3.2. `shift_types` (シフト区分)
*`requirements.md`に記載されている18パターンのシフト（早1, 夜1, 休, 明, 1〜8など）を登録します。*